
2019-10-18

设计一个控制台项目，声明一个学生类Student，包含学生学号、姓名、和分数等成员。另外声明一个可美剧类MyClass，包含List<Student>对象list字段，向其中添加若干学生的成绩，使用get访问器（即属性方式）设计两个迭代器，分别多用于迭代合格（分数大于等于60）和不合格（分数小于60）的学生记录，在Main方法中输出这两个迭代器的学生记录，并采用相关数据进行测试。